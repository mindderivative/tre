# Getting Started

*This page is a stub -- content coming as the documentation rewrite proceeds.*

Planned contents:

- Prerequisites (Rust toolchain, Python 3.x, a Vulkan-capable GPU/driver)
- Building `tre-python` locally with `maturin develop`
- Rendering your first frame: `Canvas`, `ShapeRegistry`, `HeadlessRenderer`
- Rendering to a real window with `WindowedRenderer`
- Where to go next (Architecture, Python API)