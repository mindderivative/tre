# Platform Integration

*This page is a stub -- content coming as the documentation rewrite proceeds.*

Planned contents:

- Clipboard (`tre.Clipboard`)
- Native file dialogs (`pick_file`, `pick_files`, `pick_folder`, `save_file`)
- System tray and native menus (`tre.TrayIcon`, `tre.Menu`)
- Accessibility (`tre.A11yBridge`, AT-SPI2 on Linux)
- Drag-and-drop and IME passthrough
- Window chrome (title, min/max, icon) and multi-monitor support
- Known platform-specific limits (Linux-only surfaces, Wayland-specific no-ops)