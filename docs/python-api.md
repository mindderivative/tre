# Python API

*This page is a stub -- content coming as the documentation rewrite proceeds.*

Planned contents:

- `Canvas`, `ShapeRegistry`, and the shape primitives (`Rectangle`, `Circle`, `Polygon`, `Path`, `Text`, `Svg`)
- Fill styles: solid color, gradients, textures
- `EditableText`: caret, selection, IME
- Animation: `Clock`, `Tween`, `Easing`, `Spring`, `Timeline`
- Custom shaders (`create_custom_shader`, `CustomShaded`)
- Rendering: `HeadlessRenderer` and `WindowedRenderer`
- Input events (`InputEvent` and its variants)
- Focus and keyboard navigation (`FocusManager`, `Canvas.tag_focusable`)