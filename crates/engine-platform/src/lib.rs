//! `winit` `EventLoop`/`ApplicationHandler` and the `accesskit_winit`
//! adapter.
//!
//! Placeholder -- scaffolded in M3 Phase 1 (workspace scaffold). Real
//! content lands at build-order step 1 (ARCHITECTURE.md §14, as a dev
//! dependency of engine-render's own example) and is wired for real
//! starting at step 7 (accesskit) / step 14 (multi-window).
